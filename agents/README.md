# Agents

This directory describes agent identities and expected behavior.

Agents do not need complex registration to use Maat. A stable identity is enough for ledger attribution and Git commit authorship.

Use `agents/_template.md` when an agent needs a persistent profile.
