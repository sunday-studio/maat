# Projects

Each file in this directory represents the current state of one project.

Use `projects/_template.md` for new projects.

Project files are not the transaction log. They are the current readable state. Every meaningful change here should have a matching event in `ledger/`.
