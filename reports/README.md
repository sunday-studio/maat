# Reports

Reports are summaries, handoffs, and digests written by agents.

They are useful for human review and cross-project awareness, but they are not the source of truth. Source-of-truth state belongs in `projects/`, and transactional history belongs in `ledger/`.
